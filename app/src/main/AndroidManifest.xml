<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ros.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_pictlogo"
        android:label="HI"
        android:roundIcon="@mipmap/ic_pictlogo_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".onboard2"></activity>
        <activity android:name=".onboard1" />
        <activity android:name=".ProfilScreen" />
        <activity android:name=".form" />
        <activity android:name=".regist" />
        <activity android:name=".home" />
        <activity android:name=".splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>